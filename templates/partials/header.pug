header.header(class= title ? "header--" + title.replace(/'/g, "").replace(/\s/g, "-").toLowerCase() : "")
  .header__text
    if headline
      h1= headline
    else
      h1= title
    if description
      h3!= description
